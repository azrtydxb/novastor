---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: storagequotas.novastor.io
spec:
  group: novastor.io
  names:
    kind: StorageQuota
    listKind: StorageQuotaList
    plural: storagequotas
    singular: storagequota
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.scope.kind
      name: Scope
      type: string
    - jsonPath: .spec.scope.name
      name: Scope Name
      type: string
    - jsonPath: .spec.storage.hard
      name: Storage Limit
      type: string
    - jsonPath: .status.storage.used
      name: Used
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          StorageQuota defines storage consumption limits for a scope.
          Scopes can be namespaces (tenant-level), specific storage pools, or individual buckets.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              objectCount:
                description: ObjectCount defines object/bucket count limits.
                properties:
                  hard:
                    description: Hard is the hard limit for object/bucket count.
                    format: int64
                    type: integer
                required:
                - hard
                type: object
              scope:
                description: Scope defines the target of this quota.
                properties:
                  kind:
                    enum:
                    - Namespace
                    - StoragePool
                    - Bucket
                    - Volume
                    type: string
                  name:
                    description: |-
                      Name identifies the scope target.
                      For Namespace: the namespace name
                      For StoragePool: the pool name
                      For Bucket: the bucket name
                      For Volume: the volume ID
                    type: string
                required:
                - kind
                - name
                type: object
              storage:
                description: Storage defines storage limits.
                properties:
                  hard:
                    description: Hard is the hard limit in bytes. Requests exceeding
                      this limit are rejected.
                    format: int64
                    type: integer
                  soft:
                    description: Soft is the soft limit in bytes. When exceeded, warnings
                      are issued but requests are allowed.
                    format: int64
                    type: integer
                required:
                - hard
                type: object
            required:
            - scope
            type: object
          status:
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              objectCount:
                description: ObjectCount reports object/bucket count usage.
                properties:
                  used:
                    description: Used is the current count.
                    format: int64
                    type: integer
                type: object
              phase:
                description: Phase indicates the quota enforcement state.
                enum:
                - Active
                - Exceeded
                - Warning
                type: string
              storage:
                description: Storage reports storage usage.
                properties:
                  used:
                    description: Used is the current usage in bytes.
                    format: int64
                    type: integer
                  usedString:
                    description: UsedString is the current usage in human-readable
                      format.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
