apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: example-block-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: novastor-block
  resources:
    requests:
      storage: 10Gi
