apiVersion: novastor.io/v1alpha1
kind: BlockVolume
metadata:
  name: postgres-data
spec:
  pool: nvme-replicated
  size: "100Gi"
  accessMode: ReadWriteOnce
  dataProtection:
    mode: replication
    replication:
      factor: 3
      writeQuorum: 2
