# syntax=docker/dockerfile:1.7

# ============================================================
# Build stage
# ============================================================
FROM golang:1.24-alpine AS builder

LABEL stage=builder

RUN apk add --no-cache git ca-certificates tzdata

WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

COPY . .

ARG VERSION=dev
ARG COMMIT=none
ARG BUILD_DATE=unknown

# GOTOOLCHAIN=local prevents Go from attempting to download toolchain 1.25.
RUN GOTOOLCHAIN=local CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
    go build \
      -trimpath \
      -ldflags="-s -w \
        -X main.version=${VERSION} \
        -X main.commit=${COMMIT} \
        -X main.date=${BUILD_DATE}" \
      -o /out/novastor-controller \
      ./cmd/controller

# ============================================================
# Runtime stage
# ============================================================
FROM gcr.io/distroless/static-debian12:nonroot

LABEL org.opencontainers.image.title="novastor-controller" \
      org.opencontainers.image.description="NovaStor Kubernetes operator / controller manager" \
      org.opencontainers.image.source="https://github.com/piwi3910/novastor" \
      org.opencontainers.image.licenses="Apache-2.0"

COPY --from=builder /out/novastor-controller /usr/local/bin/novastor-controller
COPY --from=builder /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/

# Prometheus metrics and health probes
EXPOSE 8080
# Kubernetes webhook server
EXPOSE 9443

ENTRYPOINT ["/usr/local/bin/novastor-controller"]
